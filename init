YouTube AI Video Summarizer Chrome extension. Detects YouTube videos, grabs transcripts, lets user pick an AI provider (Claude, GPT, Chrome Prompt/Summarization APIs, etc.), sends the transcript for summarizing, and shows the result in the popup. Core files: manifest.json, popup.html/js, content.js, background.js (service worker), styles.css, icons, config.js. Load unpacked in Chrome, supply API keys, respect rate limiting and transcript validation.
